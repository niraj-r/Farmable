<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.niraj.tester" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Startup"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Login"
            android:label="Log In"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".MainActivity"
            android:label="Main Activity"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".PlantProfile"
            android:label="Plant Profile"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".PlantSearch"
            android:label="Plant Search"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".GardenTomato"
            android:label="Garden Tomato"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".Potato"
            android:label="Potato"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".Rosemary"
        android:label="Rosemary"
        android:theme="@style/AppTheme"></activity>

        <activity android:name=".Basil"
            android:label="Basil"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".Carrot"
            android:label="Carrot"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".Lettuce"
            android:label="Lettuce"
            android:theme="@style/AppTheme"></activity>

    </application>

</manifest>
